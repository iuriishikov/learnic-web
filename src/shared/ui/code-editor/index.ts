// Public surface of the code-editor package — mirrors the legacy
// `code-editor.tsx` exports exactly so every existing consumer
// continues to import from `@/shared/ui/code-editor` without change.
export { CodeEditor } from './code-editor';
export type { CodeEditorProps, EditorTab } from './types';
