// Public surface of the rich-editor package — mirrors the legacy
// `rich-editor.tsx` exports exactly so every existing consumer
// continues to import from `@/shared/ui/rich-editor` without change.
export { RichEditor, type RichEditorProps } from './rich-editor';
